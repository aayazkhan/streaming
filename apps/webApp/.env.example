# Base path for API requests. Leave as /v1 to use the Vite dev proxy (see vite.config.ts)
# or the production nginx reverse proxy (see nginx.conf) — both forward /v1 to the api-gateway.
VITE_API_BASE_URL=/v1
